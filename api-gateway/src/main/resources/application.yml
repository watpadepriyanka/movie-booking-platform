server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:

        - id: show-service
          uri: http://localhost:8082
          predicates:
            - Path=/shows/**

        - id: seat-service
          uri: http://localhost:8083
          predicates:
            - Path=/seats/**